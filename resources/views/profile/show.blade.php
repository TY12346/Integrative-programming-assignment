@extends('layouts.app') @section('content')<h1>Profile</h1><form method="post">@csrf<input class="form-control mb-2" name="full_name" value="{{ auth()->user()->full_name }}"><input class="form-control mb-2" name="phone_no" value="{{ auth()->user()->phone_no }}"><textarea class="form-control mb-2" name="address">{{ auth()->user()->partnerProfile->address ?? '' }}</textarea><button class="btn btn-success">Save</button></form>@if(auth()->user()->role!=='ADMIN')<hr><h3>Upload verification document</h3><form method="post" action="/profile/document" enctype="multipart/form-data">@csrf<input class="form-control mb-2" name="document_type" placeholder="Document type"><input class="form-control mb-2" type="file" name="document"><button class="btn btn-outline-success">Upload</button></form>@endif@endsection
