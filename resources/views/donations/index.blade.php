@extends('layouts.app') @section('content')<h1>My Donations</h1><a class="btn btn-success mb-2" href="/donations/create">Create donation</a><table class="table">@foreach($donations as $d)<tr><td>{{ $d->food_name }}</td><td>{{ $d->current_quantity }} {{ $d->measurement_unit }}</td><td>{{ $d->donation_status }}</td><td><a href="/donations/{{ $d->donation_id }}/edit">Edit</a><form method="post" action="/donations/{{ $d->donation_id }}/cancel">@csrf<button class="btn btn-sm btn-warning">Cancel</button></form></td></tr>@endforeach</table>@endsection
