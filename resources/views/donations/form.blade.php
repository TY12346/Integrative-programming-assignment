@extends('layouts.app') @section('content')<h1>{{ $donation->exists ? 'Edit' : 'Create' }} Donation</h1><form method="post" enctype="multipart/form-data" action="{{ $donation->exists ? '/donations/'.$donation->donation_id : '/donations' }}">@csrf @if($donation->exists) @method('PUT') @endif<select class="form-select mb-2" name="category_id">@foreach($categories as $c)<option value="{{ $c->category_id }}" @selected($donation->category_id==$c->category_id)>{{ $c->category_name }}</option>@endforeach</select><input class="form-control mb-2" name="food_name" value="{{ $donation->food_name }}" placeholder="Food name"><textarea class="form-control mb-2" name="description" placeholder="Description">{{ $donation->description }}</textarea><input class="form-control mb-2" name="donation_quantity" value="{{ $donation->donation_quantity }}" placeholder="Quantity"><input class="form-control mb-2" name="measurement_unit" value="{{ $donation->measurement_unit }}" placeholder="Unit"><input class="form-control mb-2" type="datetime-local" name="expiry_datetime"><input class="form-control mb-2" name="pickup_address" value="{{ $donation->pickup_address }}" placeholder="Pickup address"><input class="form-control mb-2" name="storage_type" value="{{ $donation->storage_type }}" placeholder="Storage type"><input class="form-control mb-2" name="halal_status" value="{{ $donation->halal_status }}" placeholder="Halal status"><input class="form-control mb-2" type="file" name="photo"><button class="btn btn-success">Save</button></form>@endsection
