@extends('layouts.app') @section('content')<h1>Available Donations</h1><form class="mb-3"><select name="category_id" class="form-select"><option value="">All categories</option>@foreach($categories as $c)<option value="{{ $c->category_id }}">{{ $c->category_name }}</option>@endforeach</select><button class="btn btn-success mt-2">Search</button></form>@foreach($donations as $d)<div class="card mb-2"><div class="card-body"><h5>{{ $d->food_name }}</h5><p>{{ $d->current_quantity }} {{ $d->measurement_unit }}. Expires {{ $d->expiry_datetime }}.</p></div></div>@endforeach@endsection
