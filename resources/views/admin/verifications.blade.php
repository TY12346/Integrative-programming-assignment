@extends('layouts.app') @section('content')<h1>Verification Reviews</h1>@foreach($profiles as $p)<div class="card mb-2"><div class="card-body"><h5>{{ $p->user->full_name }} - {{ $p->verification_status }}</h5><ul>@foreach($p->documents as $d)<li>{{ $d->document_type }}: {{ $d->file_path }}</li>@endforeach</ul><form method="post" action="/admin/verifications/{{ $p->profile_id }}">@csrf<select name="decision"><option>APPROVED</option><option>REJECTED</option></select><input name="remarks" placeholder="Remarks"><button class="btn btn-success">Review</button></form></div></div>@endforeach@endsection
