@extends('layouts.app') @section('content')<h1>Food Requests</h1><a class="btn btn-success mb-2" href="/requests/create">Create request</a><table class="table">@foreach($requests as $r)<tr><td>{{ $r->category->category_name }}</td><td>{{ $r->fulfilled_quantity }}/{{ $r->requested_quantity }} {{ $r->unit }}</td><td>{{ $r->request_status }}</td><td><a href="/requests/{{ $r->request_id }}/edit">Edit</a> <a href="/requests/{{ $r->request_id }}/reservations">Reservations</a><form method="post" action="/requests/{{ $r->request_id }}/cancel">@csrf<button class="btn btn-sm btn-warning">Cancel</button></form></td></tr>@endforeach</table>@endsection
