@extends('layouts.app') @section('content')<h1>{{ $foodRequest->exists ? 'Edit' : 'Create' }} Food Request</h1><form method="post" action="{{ $foodRequest->exists ? '/requests/'.$foodRequest->request_id : '/requests' }}">@csrf @if($foodRequest->exists) @method('PUT') @endif<select class="form-select mb-2" name="category_id">@foreach($categories as $c)<option value="{{ $c->category_id }}">{{ $c->category_name }}</option>@endforeach</select><input class="form-control mb-2" name="requested_quantity" value="{{ $foodRequest->requested_quantity }}" placeholder="Quantity"><input class="form-control mb-2" name="unit" value="{{ $foodRequest->unit }}" placeholder="Unit"><input class="form-control mb-2" type="datetime-local" name="request_deadline"><button class="btn btn-success">Save</button></form>@endsection
